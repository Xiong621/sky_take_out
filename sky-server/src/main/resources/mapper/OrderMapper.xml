<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into  orders(number, status, user_id, address_book_id, order_time, checkout_time, pay_method, pay_status
            ,amount, remark, phone, address, consignee, estimated_delivery_time, delivery_status
                           ,pack_amount, tableware_number, tableware_status)
        VALUES (#{number},#{status},#{userId},#{addressBookId},#{orderTime},#{checkoutTime},#{payMethod},#{payStatus}
               ,#{amount},#{remark},#{phone},#{address},#{consignee},#{estimatedDeliveryTime}
               ,#{deliveryStatus},#{packAmount},#{tablewareNumber},#{tablewareStatus})
    </insert>
    <select id="pageQueryOrder" resultType="com.sky.entity.Orders">
        select * from orders
        <where>
            <if test="beginTime != null">begin_time = #{beginTime}</if>
            <if test="endTime != null">begin_time = #{endTime}</if>
            <if test="number != null">number = #{number}</if>
            <if test="phone != null">phone = #{phone}</if>
            <if test="status != null">status = #{status}</if>
        </where>
        order by order_time desc
    </select>
</mapper>